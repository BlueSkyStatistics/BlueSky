#replaces . and space in dataset names
replacePeriodWithUnderscore<-function (dataset =name)
{
#cat("\nChanging col names")
#dataset = eval (parse(text =paste(dataset)))

# print(names(iris))
# eval(parse(text=paste('temp <- colnames(',dataset,')')))
# print(temp)
# eval(parse(text=paste('tempVar=gsub(".", "_",temp,fixed=TRUE)'))) # replace '.' with underscore
# print(tempVar)
# eval(parse(text=paste('newTemp=gsub(" ", "_",tempVar,fixed=TRUE)'))) # replace space with underscore
# print(newTemp)
# eval(parse(text=paste('colnames(',dataset,') <- newTemp')))
# print(names(iris))
 #colnames(dataset) <<- newTemp
 
#print(newTemp)
#cat("\nChanging col names done.")
}